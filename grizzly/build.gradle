mainClassName='com.menelic.jwcb.grizzly.Main'

configurations {
    compile.exclude module: "spring-boot-starter-tomcat"
}

dependencies {
    compile(project(':common'))
    compile libraries.grizzly
}