mainClassName='com.menelic.jwcb.jetty.Main'

configurations {
    compile.exclude module: "spring-boot-starter-tomcat"
}

dependencies {
    compile(project(':common'))
    compile "org.springframework.boot:spring-boot-starter-jetty:${bootVersion}"
}
